plugins { id 'java-library' }

dependencies {
    // Адаптер реализует интерфейсы из core и использует его типы — делаем транзитивно доступным
    api project(':modules:core')

    testImplementation "org.junit.jupiter:junit-jupiter:5.10.3"
}
